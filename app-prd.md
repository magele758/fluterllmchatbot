帮我写一个fluter跨端程序，完成如下功能


## 功能描述
1. 主界面使用对话框模式
2. 可以选择是否开启深度思考
3. 右上角有设置按钮，可以让用户自己配置自己的模型商提供的模型，需要能设置代理url，以及模型名称，其中添加一个模型是否支持多模态的选项，针对多模态场景生效
4. 可以支持对话模式、图片生成、视频生成、音乐生成
5. 支持一次性回话，或者存储到云端的回话，存储在微信支持的oss隔离空间中
6. 支持用户通过微信扫码登录


## 生成步骤
1. 先把我的要求拆解成产品需求文档，保存到本地
2. 把需求拆解成一个个工作事项，并记录状态，初始化均为未完成，每完成一个任务，记录当前任务为完成
3. 帮我生成一个视觉稿，以figma的fig的格式保存到本地，要求给出pc和手机端的样式



